# Leave empty for local dev (Vite proxies /api to localhost:5000).
# Production: https://your-api.onrender.com
VITE_API_URL=

# Mini App is the same Vite build. Serve it over HTTPS and register
# that URL in BotFather (/newapp) and TELEGRAM_WEBAPP_URL on the API.
